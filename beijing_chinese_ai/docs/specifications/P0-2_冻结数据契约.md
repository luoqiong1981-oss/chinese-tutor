继续实施「北京语文 AI 辅导系统」。

当前工程根目录已经确定为：

D:/1_Link-KnowledgeBase/1.北京语文AI辅导/beijing_chinese_ai/

不要迁移工程。
不要新建第二套工程。

当前：

P0-SCAFFOLD 已完成。

现在进入：

P0-2 数据契约冻结与自动验收。

==================================================
一、本阶段原则
==================================================

这不是继续写研究报告。

本阶段要真正创建：

1. JSON Schema
2. 主数据文件
3. 自动校验脚本
4. 自动化测试
5. P0-2 验收报告

所有字段和能力定义必须优先从项目中已有的研究报告、
specifications、能力模型、数据字典等资料中提取。

禁止为了“补完整”自行编造能力名称、定义、证据来源或政策依据。

如果项目现有资料不足以确定某字段内容：

使用 null、unknown 或 TODO，
并在验收报告中列出缺失项。

不得猜测。

==================================================
二、先扫描现有资料
==================================================

首先搜索：

docs/research/
docs/specifications/

以及工程根目录已有的相关研究文件。

重点寻找：

- 39 项能力模型
- 能力 ID
- 能力名称
- 能力定义
- 操作领域
- 子技能
- 能力证据
- 北京中高考能力要求
- 课程标准依据
- baseline_status
- skill_state
- mastery / 迁移等级
- 学情记录规范

先输出内部识别结果，然后再创建数据。

不得重新设计一套新的能力模型。

==================================================
三、冻结能力 ID
==================================================

能力总数必须严格为 39。

固定 ID：

BA01
BA02
BA03
BA04

LU01
LU02
LU03
LU04

RD01
RD02
RD03
RD04
RD05
RD06
RD07
RD08

CL01
CL02
CL03
CL04

PO01
PO02
PO03
PO04

WR01
WR02
WR03
WR04
WR05
WR06

OC01
OC02
OC03

MC01
MC02
MC03

WB01
WB02
WB03

领域：

BA
LU
RD
CL
PO
WR
OC
MC

architecture_type =
operational_domain

WB

architecture_type =
cross_domain_module

禁止：

- 增加第 40 项
- 删除能力
- 修改 ID
- 重用 ID
- 把 WB 当成普通学科领域
- 在没有原设计依据时自行改能力名称

==================================================
四、创建 Schema
==================================================

在 schemas/ 中创建：

1.
ability_catalog.schema.json

定义单项能力的数据契约。

至少支持以下字段：

ability_id
domain_code
architecture_type
ability_name
definition
assessment_focus
evidence_requirements
source_refs
version
status

要求：

ability_id 为永久主键。

domain_code 必须限定为：

BA
LU
RD
CL
PO
WR
OC
MC
WB

architecture_type 只能：

operational_domain
cross_domain_module

WB 必须为：

cross_domain_module

其他领域必须：

operational_domain


2.
subskills.schema.json

用于能力下的子技能。

至少：

subskill_id
ability_id
subskill_name
definition
observable_behavior
source_refs
status


3.
evidence_sources.schema.json

用于记录证据来源。

至少支持：

source_id
source_type
title
issuer_or_author
year
document_location
page_or_section
authority_level
notes

source_type 可包括：

official_policy
curriculum_standard
exam
official_exam_analysis
research
class_material
original_text
other


4.
claims.schema.json

用于记录“系统结论—证据”的关系。

至少支持：

claim_id
claim_text
claim_type
ability_ids
source_ids
evidence_strength
notes


5.
student_profile.schema.json

注意这里只定义数据契约，
不要创建真实学生数据。

至少：

student_id
display_alias
grade_stage
baseline_status
created_at
updated_at

student_id 不能使用真实姓名。

baseline_status 默认：

unknown


6.
skill_state.schema.json

用于长期能力状态。

至少：

student_id
ability_id
skill_state
confidence
evidence_count
last_evaluated_at
next_review_at

skill_state 使用：

S0
S1
S2
S3
S4
S5
S6

特别注意：

“尚未测试”
绝对不能自动写成：

S0

尚未测试必须通过：

baseline_status = unknown

表达。


7.
learning_event.schema.json

这是后面所有真实辅导数据的核心。

至少支持：

event_id
session_id
student_id
timestamp
domain_code
ability_ids
task_id
event_type
student_response
evidence_used
hint_level
result
error_type
transfer_level
teacher_or_ai_note

event_type 至少支持：

diagnosis
instruction
practice
transfer
feynman
review


8.
session.schema.json

至少：

session_id
student_id
started_at
ended_at
session_type
target_ability_ids
learning_event_ids
summary
next_review_ability_ids


9.
task.schema.json

用于以后诊断题/训练题/迁移题。

至少：

task_id
task_type
domain_code
ability_ids
difficulty
material_source
prompt
answer_reference
evidence_reference
status

当前只建 Schema。

不要批量生成题目。

==================================================
五、建立第一版主数据
==================================================

在：

data/master/

创建：

ability_catalog.v1.1.json

要求：

必须恰好包含 39 项能力。

能力名称、定义、assessment_focus 等信息：

优先从现有研究和 specifications 中提取。

如果某个能力资料不足：

不要编造。

允许：

definition = null

并增加：

"needs_review": true

但：

ability_id
domain_code
architecture_type

必须完整。

同时创建：

master_data_version.json

记录：

schema_version
ability_catalog_version
created_at
status
breaking_change_policy

breaking_change_policy 中明确：

能力 ID 为永久主键。

以后即使能力弃用：

只能 deprecated，
不能删除后把 ID 给其他能力使用。

==================================================
六、建立学生状态规则
==================================================

在：

docs/specifications/

创建：

student_state_rules.md

明确：

baseline_status 和 skill_state 是两个维度。

baseline_status 可以：

unknown
in_progress
established

skill_state：

S0-S6

只有存在真实学习证据后才能写入 skill_state。

定义 S0-S6 时：

优先读取已有研究和系统设计。

如果旧资料已经有明确版本：

直接采用。

如果没有明确依据：

不要自行定义，
标记为：

PENDING_DEFINITION

并在验收报告指出。

==================================================
七、自动校验器
==================================================

创建：

scripts/validate_v1_1.py

运行后至少自动检查：

A. ability_catalog 是否存在

B. 能力数量是否正好为：

39

C. ID 是否完全等于冻结列表

D. 是否有重复 ability_id

E. domain_code 是否正确

F. architecture_type 是否正确

G. WB 是否全部为：

cross_domain_module

H. 其他领域是否全部为：

operational_domain

I. 所有 Schema 是否为合法 JSON

J. ability_catalog 是否能通过：

ability_catalog.schema.json

K. 跨表 ability_id 引用是否有效

L. student_profile 是否禁止直接使用真实姓名作为主键

M. baseline_status 是否允许 unknown

N. 是否存在：

unknown -> S0

这种错误初始化逻辑

O. master_data_version 是否存在

最终输出：

PASS / FAIL

以及每项检查结果。

==================================================
八、测试
==================================================

在 tests/ 创建自动测试。

至少测试：

1.
39 个能力全部存在

2.
删除一个能力时校验失败

3.
增加第 40 个能力时校验失败

4.
重复 ability_id 时失败

5.
WB architecture_type 写错时失败

6.
RD architecture_type 写成 cross_domain_module 时失败

7.
baseline_status = unknown 合法

8.
未经过学习事件就初始化 S0 的测试应失败

9.
不存在 ability_id 的跨表引用必须失败

允许使用匿名 mock 数据进行测试。

但 mock 数据只能放：

tests/fixtures/

或：

data/examples/

不得写入：

data/students/

==================================================
九、README 更新
==================================================

更新 README.md。

增加当前工程状态：

P0-1 Scaffold
PASS

P0-2 Data Contract
当前状态

并解释：

研究完成 ≠ 系统建成

当前正在建立：

研究
→ 数据契约
→ 自动验收
→ 诊断
→ 辅导
→ 学情
→ 复测

的数据闭环。

==================================================
十、P0-2 验收
==================================================

创建：

docs/acceptance/P0-2_DATA_CONTRACT.md

必须记录：

1. 创建的 Schema
2. 主数据数量
3. 39 项能力检查结果
4. 引用完整性检查
5. baseline_status 检查
6. skill_state 检查
7. 隐私规则检查
8. 自动测试结果
9. 尚未解决的问题
10. 下一阶段建议

然后实际运行：

python scripts/validate_v1_1.py

以及测试。

如果安装了 pytest：

pytest

如果没有：

使用 Python 标准库 unittest，
不要为了测试强制安装大型依赖。

==================================================
十一、完成后只给我工程结果
==================================================

完成后输出：

【P0-2 数据契约验收】

Schema：
x/x PASS

能力数量：
39/39 PASS 或 FAIL

能力 ID：
PASS / FAIL

architecture_type：
PASS / FAIL

跨表引用：
PASS / FAIL

baseline_status：
PASS / FAIL

skill_state 初始化规则：
PASS / FAIL

隐私规则：
PASS / FAIL

自动测试：
x passed / x failed

待人工确认：
列出所有 TODO / PENDING_DEFINITION

最终结论：

P0-2 DATA CONTRACT = PASS

或：

P0-2 DATA CONTRACT = FAIL

如果 FAIL：
不要为了得到 PASS 擅自修改研究结论。
必须说明具体缺什么资料。